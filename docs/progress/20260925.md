# 2026-09-25

## 12:30 · Consolidation, Gemini rewrite, 3D demo, first deploy

**What changed**

- Consolidated the three hackathon repos (`TeachXR-frontend`,
  `TeachXR-backend-dataflow`, `TeachXR-backend-gesture`) into this repo, as a
  rewrite rather than a code import.
- **Backend** (FastAPI):
  - Replaced Vapi, Cloud Vision OCR, SingleStore and socket.io with a Gemini
    Live relay (`gemini-3.8-live`, verified available on the new key).
  - Verified that circled images must go in as `send_client_content` inline
    parts, not realtime video frames, and that realtime text/audio and client
    content mix fine in one session.
  - Offline tests are in `backend/tests/test_live.py` (7 passing).
- **Frontend**: a new Three.js dorm-desk scene with:
  - Procedural room and props, plus 7 CC0 Poly Haven models (1.4 MB).
  - A flippable 5-topic book with matching monitor screens.
  - A glasses "put on" animation, a boot sequence, and a holographic HUD
    (chat, audio-reactive orb, dock).
  - A lasso capture with book/monitor detection, and quick questions.
- Verified locally in the browser pane:
  1. Put on the glasses.
  2. Boot.
  3. The Gemini greeting arrives.
  4. Circle the left page of "Our Solar System" and choose Explain. Gemini
     correctly described the passage.
  5. A typed follow-up gets an answer.
  6. The page flips.
- **Deployed** to vietrochack-lab:
  - Cloud Run `teachxr-server` and the Hosting site `vietrochack-teachxr`.
  - Firestore database `teachxr` with TTL, and the AR repo `teachxr` with a
    cleanup policy.
  - The secret, and WIF for CI.
  - Prod smoke test: a session through Hosting, then the WebSocket direct to
    Cloud Run, returned the spoken greeting.
  - Bad origin → 403.

**Decisions**: ADRs 0001–0005.

**Open TODOs**

- DNS records for `teachxr.vietrochack.com` (see runbook) need adding at Namecheap.
- AI Studio spend cap for the key.
- Revoke the Vapi private key that was committed to the public `TeachXR-frontend` repo.
- Real-device mic QA (backlog).
- Archive the old repos with "moved" READMEs; add TeachXR to `VietRocHack/home`.

## 14:30 · Tutor window floats in the room

- Per feedback ("the window was floating in the room"), moved the chat from a
  screen-fixed sidebar/bottom sheet into a 3D-anchored window
  (`scene/WorldUI.jsx`, drei `<Html transform>`), draggable by its header,
  with a Recenter dock button.
- Circles are now traced onto the book or monitor surface, and the capture
  card floats beside them.
- Removed the camera view-offset hack that framed the desk left of the sidebar.
- Verified in the browser pane: the window appears after boot; circling the
  monitor gives a 🖥️ Monitor capture and a Socratic answer streams into the
  floating window; dragging moves it; the phone layout works. ADR 0004 is updated.

## 15:30 · The floating window is the original /learn page

- Per feedback ("keep the avatar and the control buttons, exactly how it looks
  in the old website"), replaced the new chat panel with a port of the original
  Learn page (`hud/LegacyLearnPanel.jsx`): the gradient, the orb and avatar,
  the NavigationBar with its center call button, and the black ChatBox with
  newest-first bubbles.
- Wired it to Gemini: ＋ connects, Turn Off hangs up (the orb comes back), Mute
  toggles the mic, Home takes the glasses off. The window is moved by a
  visionOS-style bar under it. The default placement is left of the book, so
  the book and monitor stay clear.
- Fix: App now reads live R3F state (`get()`), not a stale snapshot, so Recenter
  and lasso captures use the current size after a resize.
- Removed the unused `ChatPanel.jsx` and `Orb.jsx`.

## 15:50 · The tutor starts only when asked

- Per feedback, putting the glasses on no longer connects to Gemini. The boot
  sequence says "tutor standing by", and the window opens idle (orb, "Touch to
  empower your learning"). The student presses the blue ＋ call button to start,
  like the original site, and that click is also the gesture that allows the
  mic prompt and audio. The status bar says STANDBY until then. The capture
  card and the landing's "How it works" point to ＋.
- Added fallback timers so the wear and take-off animations can't stall the
  flow on a hidden tab or a slow GPU.

## 17:00 · Room remodel and chat order

- The chat now runs oldest to newest, top to bottom, with autoscroll and the
  text box at the bottom.
- Rebuilt the room for realism (ADR 0004 is updated):
  - PBR surfaces.
  - About 20 CC0 Poly Haven models: bed, pillows, nightstand, shelves, plants,
    frames, armchair, side table, dresser, pendant.
  - Café curtains and two strings of fairy lights.
  - Motivated lighting, a dim HDRI, N8AO, AgX, and VSM soft shadows.
- Found along the way:
  - drei `SoftShadows` breaks every lit shader on three r186, so VSM is used.
  - The fleece texture's color map is too dark for a duvet; only its normals
    are used.
  - The new book sets needed to sit on the bookshelf's measured shelf heights.
- Dev-only `window.__snap(width)` overlays a post-processed frame (screenshots
  of the preview pane were cropped at 1.5x DPR), and `window.__cam` takes over
  the camera.
- Assets now total about 9.5 MB.
